/**
 * Stable type key for this plugin's sidebar item. The
 * `plugin:<plugin-name>:<id>` convention keeps plugin contributions
 * from colliding with built-in `OptionType` values or with other
 * plugins.
 */
// eslint-disable-next-line import/prefer-default-export
export const COLUMN_INSPECTOR_ITEM_TYPE =
  'plugin:table-options-example:column-inspector';
